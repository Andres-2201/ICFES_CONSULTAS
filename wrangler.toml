name = "icfes-consultas"
main = "src/worker.js"
compatibility_date = "2025-09-01"

# Sirve el frontend estático generado por Vite (carpeta dist/).
# El Worker (src/worker.js) maneja /api/consulta.
[assets]
directory = "./dist"
binding = "ASSETS"
# SPA: si la ruta no existe como archivo, sirve index.html.
not_found_handling = "single-page-application"
